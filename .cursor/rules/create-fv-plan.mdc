---
description: 为新功能或重构创建 FV-First 执行计划。先产出中文讨论文档（无代码），等待人类评审，再生成完整的 FV_EXECUTION_PLAN.md。
alwaysApply: false
---

> **用法**：描述要规划的功能或重构即可触发。内部推理可用英文；两份输出文档必须用中文。

## Phase 1 — 讨论文档（先产出，再暂停）

**探索**：读 `pyproject.toml` 提取 package/module 拓扑；读所有涉及 package 的 `__init__.py`；搜索现有类似实现；识别将触及的公开 API（breaking-change 风险锚点）；检查 `tests/strategies.py` 是否存在并读取已有 Strategy。

**输出 `PLAN_DISCUSSION.md`**（无实际代码，使用架构语义）：

1. 背景与目标
2. 现状拓扑（package/module 现状、架构痛点）
3. 演进方案（实现路径、状态机变更）
4. 验证策略（哪些约束用类型捕获、哪些用契约表达、哪些用 Hypothesis 属性证明、哪些用 pytest 示例回归）
5. 爆炸半径（涉及 package 和文件、`tests/strategies.py` 是否需要扩充）
6. 约束与防线（CONVENTION、并发假设、性能天花板）
7. Phase 划分（阶段目标，不要 Step 细节）
8. 待决议断言（需人类确认的未知常量或业务边界）

完成后提示：> 以上为架构草案。如有修正请指出；若无异议，回复「确认」，我将生成 `FV_EXECUTION_PLAN.md`。

## Phase 2 — 完整执行计划（仅在人类确认后）

生成 `FV_EXECUTION_PLAN.md`，必须包含：

**标题**：`# FV 执行计划：<功能标题>`

**全局规则**：引用 workspace 标准（Writer/Reviewer 隔离、熔断 3 次、Gate Check、SPECIFY→IMPLEMENT→VERIFY）。

**工作区拓扑**：涉及 package、职责、不可变 API 边界。

**Strategy 状态**：`tests/strategies.py` 现有 Strategy 清单；本次新增的 Strategy 名称与类型。

**缺陷/技术债收敛**：现象 → 根因 → 修正态 → 触碰文件。

**Phase 概览表**：`| Phase | 变更域 | 新增验证数（类型/契约/属性/示例）|`

**每个 Step**（格式）：

```
### Step X.Y: <标题>
目标: 一句话
新建/修改文件: ...
先读文件清单: <宏观上下文（__init__.py + 领域模型）+ 待改文件 + tests/strategies.py>
API 规格:
  签名: <完整 Python 类型签名，含泛型/TypeVar>
  契约:
    require: <@icontract.require 前置条件，用自然语言描述谓词>
    ensure:  <@icontract.ensure 后置条件>
    invariant: <@icontract.invariant（如适用）>
验证矩阵:
  L0 类型: <mypy 要捕获的约束>
  L1 契约: <icontract 装饰器谓词描述>
  L2 属性: <Hypothesis property 名 + 通用命题（如 往返一致性 / 幂等性 / 单调性）>
  L3 示例: <pytest 用例名 + Given/When/Then>
Strategy 变更: <需新增到 tests/strategies.py 的自定义 Strategy，或 "无">
异常设计: <本 Step 涉及的新异常类名及继承关系，或 "无">
依赖标注: 依赖 Step X.Y | 无依赖可与 Step X.Z 并行
Review 检查项: <该 Step 专属业务逻辑验证>
```

**验证矩阵汇总**：`| Module | 新增 L0 | 新增 L1 | 新增 L2 | 新增 L3 | 合计 |`

**执行流水线**：`Phase 1: 1.1W → 1.1R → [Retry≤3] → Gate → Commit → ...`

Phase 完成协议（git 操作）见 `fv-execute` prompt。
